name: Release

on:
    release:
        types: [created]

jobs:
    ci:
        uses: zen-fs/core/.github/workflows/ci.yaml@main
        permissions:
            contents: read
            id-token: write
    release:
        uses: zen-fs/core/.github/workflows/release.yaml@main
